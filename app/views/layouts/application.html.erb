<!DOCTYPE html>
<html>
  <head>
    <title>Chris Alley | <%= yield(:title) %></title>
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div id="overall">
      <%= render 'shared/popover' %>
      <div id="container">
        <header>
          <h1>Chris Alley</h1>
          <p>I am an experienced New Zealand based web developer and database administrator. I have previously worked as a Delphi developer, SQL Server DBA, PHP web developer. I am currently employed as a Ruby on Rails web developer.</p>
        </header>
        <nav>
          <ul>
  	        <li><%= link_to "Development Portfolio", root_url %></li>
            <li><%= link_to "Industry Experience", jobs_path %></li>
            <li><%= link_to "Qualifications", qualifications_path %></li>
            <% Page.where(publish: true).each do |page| %>
              <li><%= link_to page.name, "/#{page.url}" %></li>
            <% end %>  
          </ul>
          <% if current_user %>
            <ul>
  	          <li>Logged in as <%= current_user.email_address %>.</li>
  	          <li><%= link_to "Log Out", log_out_path %></li>
              <li><%= link_to "Pages", pages_path %></li>
              <li><%= link_to "Users", users_path %></li>
            </ul>
          <% end %>
        </nav>
        <section>
          <h2><%= yield(:title) %></h2>
          <% flash.each do |name, msg| %>
            <%= content_tag :div, msg, id: "flash_#{name}" %>
          <% end %>
          <%= yield %>
        </section>
        <footer>
          <p>Copyright &copy; Chris Alley, <%= Time.now.year %>. <% if User.count == 0 %><%= link_to "Register New User", register_path %>.<% end %></p>
        </footer>
      </div>
    </div>
  </body>
</html>
